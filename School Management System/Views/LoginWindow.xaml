<Window x:Class="School_Management_System.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="450" Width="400" 
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Border Background="White" CornerRadius="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
        </Border.Effect>

        <StackPanel Margin="30">
            <Image Source="/Assets/grad_icon.png" Height="60" Margin="0,10,0,10"/>
            <TextBlock Text="Welcome Back" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#2C3E50"/>
            <TextBlock Text="Sign in to access your dashboard" Foreground="#7F8C8D" HorizontalAlignment="Center" Margin="0,0,0,30"/>

            <TextBlock Text="Email Address" Margin="0,0,0,5" FontWeight="SemiBold"/>
            <TextBox x:Name="EmailTxt" Height="35" Margin="0,0,0,15" Padding="5,7,0,0"/>

            <TextBlock Text="Password" Margin="0,0,0,5" FontWeight="SemiBold"/>
            <PasswordBox x:Name="PasswordTxt" Height="35" Margin="0,0,0,25"/>

            <Button Content="Sign In" Height="40" Background="#4A90E2" Foreground="White" 
                    FontWeight="Bold" Cursor="Hand" Click="SignIn_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>

            <TextBlock Margin="0,20,0,0" HorizontalAlignment="Center">
                <Run Text="Don't have an account?" Foreground="#7F8C8D"/>
                <Hyperlink Click="GoToSignUp_Click"> Sign Up</Hyperlink>
            </TextBlock>
        </StackPanel>
    </Border>
</Window>